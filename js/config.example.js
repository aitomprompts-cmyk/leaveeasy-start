// ─────────────────────────────────────────────────────────────
// js/config.example.js — ไฟล์ตัวอย่างค่าตั้งค่าปุ่มผู้ช่วย AI (สัปดาห์ที่ 8)
//
// 📌 วิธีใช้
//   1. คัดลอกไฟล์นี้เป็น  js/config.js
//   2. แทนค่า apiKey ด้วยคีย์ OpenRouter ที่ได้รับ
//      ขั้นตอนแบบละเอียดอยู่ในไฟล์ SETUP.md ขั้นที่ 8
//
// ⚠️ ไฟล์ js/config.js ถูก .gitignore กันไว้ **ห้าม commit เด็ดขาด**
//    คีย์นี้ใช้เรียก OpenRouter ได้จริง — ใครเห็นคีย์เอาไปใช้แทนเราได้
// ─────────────────────────────────────────────────────────────

export const aiConfig = {
  apiKey: "ใส่คีย์ OpenRouter ของคุณ",
  model: "google/gemini-2.5-flash-lite"
};
